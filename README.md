In the following file GetSomething.js we understood to fetch the api,the api we used here is public if error occoured in the api " some error occoured" will be printed .
otherwise some task will come which you have to do.
